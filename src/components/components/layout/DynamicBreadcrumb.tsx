// ==========================================
// DYNAMIC BREADCRUMB - DISABLED
// ==========================================
// This component has been disabled as per user request.
// Keeping the export for backwards compatibility in case
// it's imported elsewhere in the codebase.

export const DynamicBreadcrumb = () => {
  // Return null - no breadcrumb navigation
  return null;
};